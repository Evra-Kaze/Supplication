# Not a connect 4 script sadly. This script is simply yo connect to some wifi networks
# If you know the password to a WiFi network and are just in a shell, 
# you would need to use things like wpa_supplicant + dhclient, nmcli, a bunch of other tools.
# So, tell the wifi, the password, and just connect to it using this script, thus not having to create any config...

import argparse
import os










if __name__ == "__main__":
    pass